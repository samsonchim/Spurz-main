// Supabase config for the mobile app (React Native)
// Prefer providing these via app.config.js/app.json extra (expo-constants)
// e.g. "extra": { "SUPABASE_URL": "https://xyzcompany.supabase.co", "SUPABASE_ANON_KEY": "public-anon-key" }

const FALLBACK_URL = '' // optional local fallback
const FALLBACK_ANON = '' // optional local fallback

export const SUPABASE_URL = FALLBACK_URL
export const SUPABASE_ANON_KEY = FALLBACK_ANON
